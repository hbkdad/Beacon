Run the full test suite and report results.

```bash
cd /home/user/selfclawy/dashboard && npm test 2>&1
```

Report: number of passing tests, any failures with file:line references, and whether CI would pass.
